---
description: Dex cover selection, auto-replace, and green/gray purity borders
alwaysApply: true
---

# Dex covers

Each category shows one cover photo per species. Purity is relative to that category's required tags.

**Green border:** cover specimen tags **equal** the category's required tags (no extras), except:

- Any category except Gender: required tags plus `gender` only is still green. Basic (`[]`) also treats a stored `basic` extra tag as the empty look, so `{basic}` and `{basic, gender}` are green there.
- Gender `[gender]`: green only for `{gender}` or `{gender, basic}`.
- Gender, Hisuian Sneasel only (variant Hisuian Male / Hisuian Female): green only for `{gender, hisuian}`. Johto Sneasel Male/Female follow the normal Gender rule.
- Paldean, Tauros only: required tags plus `alternate-forme` is still green (Combat / Blaze / Aqua Breed). Paldean Wooper and other extras stay on the normal rule.

**Gray border:** species is in the category (has all required tags) but the cover is not green.

Cover rules:

- `Set as cover` is the only manual control. No pin.
- If no exact-match specimen exists, an extra-tag photo may be `Set as cover` (gray).
- When the first exact-match specimen for that category is saved, it becomes cover and the border turns green. The old cover stays in the gallery.
- After a green cover exists, do not auto-replace it. Switch only via `Set as cover`.
- When no green specimen exists, the automatic cover is the screenshot whose smallest matching category `sortOrder` is the highest. Basic (no required tags) is left out of that score. A screenshot that matches several categories is scored by the smallest `sortOrder` among them. `Set as cover` keeps the chosen photo instead of that rank.
- One specimen can be green in one category and gray in another.

```text
# Category Shadow [shadow]
# A: [shadow, shiny, tokyo]  -> gray cover
# B: [shadow]                -> auto-replaces A, green

# Same B on category Shiny Shadow [shiny, shadow] -> not in that category
```
